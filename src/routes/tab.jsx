import HomeScreen from '../screens/Home/HomeScreen';
import { defaultOptions, TabScreens } from '.';

export const TAB_ROUTE = {
  TabScreens: {
    component: TabScreens,
    options: defaultOptions,
  },
};
